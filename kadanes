
#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    int maxi=INT_MIN,i,k,j;
    int sum=0;
     for(int i=0;i<n;i++)
     {
         sum=sum+arr[i];
        if(sum<0 )
        {
            sum=0;
        }
       
        maxi=max(maxi,sum) ;
     }
      cout<<maxi;              
    
   
}
