#include<iostream> 
#include<climits>
using namespace std;
void findmax(int arr[10],int n)
{
    int max=arr[0];
    for(int j=0;j<=n;j++)
    {
        if(max<arr[j])
        {
            max=arr[j];
        }
    }
    cout<<max<<endl;
}

int main()
{
    int n;
    cin>>n;

    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    int index;
    cin>>index;
    for(int i=0;i<=index;i++)
    {
       findmax(arr,i) ;
    }
    
}
